name: tfsec
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  tfsec:
    name: tfsec
    runs-on: ubuntu-latest

    steps:
      - name: Clone repo
        uses: actions/checkout@v2
        
      - name: tfsec for critical/high risk security issues
        id: tfsec_summary
        uses: aquasecurity/tfsec-action@v1.0.0
        with:
          format: json
          additional_args: --run-statistics --out stats.json
        env:
          ACTIONS_STEP_DEBUG: true
          
      - name: Check summary file
        run: cat stats.json
      
      - name: Set tfsec output
        id: set_tfsec_output
        run: echo "::set-output name=tfsec_output1::${{ steps.tfsec_summary.outputs.tfsec_summary }}"
          
      - name: output
        if: always()
        run: echo "The tfsec output is -- ${{ steps.set_tfsec_output.outputs.tfsec_output1 }}"
          
      - name: Comment PR Summary
        uses: actions/github-script@v4
        if: always()
        with:
          script: |
            const parse_output = '${{ steps.tfsec_summary.outputs.tfsec_output1 }}'
            github.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `The following security issues were detected in the terraform file(s):<br><br>${parse_output}`
            });
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: tfsec
        id: run_sectest
        uses: aquasecurity/tfsec-action@v1.0.0
        if: always()
        with:
          soft_fail: true
          format: json
          additional_args: --out results.json 
        env:
          ACTIONS_STEP_DEBUG: true

      - name: parse output
        id: parse_output
        if: always()
        run: |
          echo ::set-output name=parse_output::$(jq -r '.results[] | ". The issue ^^ \(.rule_description) ^^ was detected in the file ^^ \(.location.filename) ^^ between the lines <u>\(.location.start_line)</u> and <u>\(.location.end_line).</u><br>"' results.json | nl)

      - name: Comment PR
        uses: actions/github-script@v4
        if: always()
        with:
          script: |
            const parse_output = '${{ steps.parse_output.outputs.parse_output }}'
            github.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `The following security issues were detected in the terraform file(s):<br><br>${parse_output}`
            })
          token: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Delete Results file
        if: always()
        run: rm -f results.json
